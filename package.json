{
  "name": "pi-meta",
  "version": "0.2.0",
  "description": "A conversational meta-channel for pi: open a linked child 'meta' session to discuss a target session, then non-destructively elide spent regions by branching the append-only session tree. Elided runs collapse to a synopsis (model + TUI); verbatim is preserved on the prior branch and expandable.",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "files": ["index.ts", "src/**/*.ts", "README.md"],
  "keywords": ["pi-package"],
  "license": "MIT"
}
